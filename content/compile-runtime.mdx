<ExplainPage id="compiler-runtime" title="程序编译与运行" summary="源码经过编译、链接、装载，最终成为进程中的机器状态变化。" layout="editorial" density="compact">
  <ConceptGraph root="program-runtime">
    <ConceptNode id="program-runtime" title="程序编译与运行" level="L0" input="源代码与运行环境" output="进程状态与外部 I/O" summary="从源码文本到 CPU 执行的完整生命周期。">
      <Overview>程序运行不是一个瞬间动作，而是编译器、链接器、操作系统和 CPU 共同完成的一条转换链。</Overview>
      <Split ratio="1.3fr .7fr">
        <Stack gap="sm">
          <RelationPath title="端到端主路径" steps={[{ node: '源码', level: '输入', tone: 'info' }, { node: '目标文件', level: '编译', relation: 'produces', tone: 'success' }, { node: '可执行文件', level: '链接', relation: 'precedes', tone: 'warning' }, { node: '进程', level: '运行', relation: 'loads', tone: 'danger' }]} />
          <Flow steps={['编译', '链接', '装载', '执行', 'I/O']} />
        </Stack>
        <Insight title="关键分界" tone="info">编译成功只代表静态约束通过；文件、权限、库版本和资源问题通常在运行期才暴露。</Insight>
      </Split>
      <NoteGrid notes={[{ title: '静态阶段', content: '语法、类型、控制流' }, { title: '组合阶段', content: '符号、段和重定位' }, { title: '动态阶段', content: '地址空间与线程' }, { title: '可观察结果', content: '输出、文件、网络、退出码' }]} />
      <Children><ConceptRef id="compile" /><ConceptRef id="link" /><ConceptRef id="runtime" /></Children>
      <Boundary>示例以 C/C++、类 Unix 和 ELF 为主；Windows PE/COFF 或字节码运行时的细节会不同。</Boundary>
    </ConceptNode>

    <ConceptNode id="compile" title="编译阶段" level="L1" parent="program-runtime" input="源文件与编译选项" output="目标文件" summary="把单个源文件逐层转换为目标平台代码。">
      <Overview>编译器前端理解语义，中端在 IR 上优化，后端再生成目标 ISA 指令。</Overview>
      <Grid columns="auto" gap="sm">
        <Mermaid title="编译流水线" width="100%" height="250px" chart={`flowchart LR
          A[源码] --> B[预处理]
          B --> C[解析]
          C --> D[语义分析]
          D --> E[IR 优化]
          E --> F[代码生成]
          F --> G[目标文件]
          style A fill:#172554,stroke:#38bdf8,color:#e0f2fe
          style G fill:#064e3b,stroke:#34d399,color:#ecfdf5`} />
        <RelationMap title="阶段产物" items={[{ from: '源码', type: '预处理', to: '.i / .ii' }, { from: '源码', type: '编译', to: '.s' }, { from: '源码', type: '汇编', to: '.o / .obj' }, { from: '多个目标文件', type: '链接', to: '可执行文件' }]} />
      </Grid>
      <Implementation language="bash" title="逐阶段观察产物">{`cc -E main.c -o main.i
cc -S main.c -o main.s
cc -c main.c -o main.o
cc main.o -o app`}</Implementation>
      <Children><ConceptRef id="parse" /><ConceptRef id="codegen" /></Children>
    </ConceptNode>

    <ConceptNode id="parse" title="解析与语义分析" level="L2" parent="compile" input="字符流与 Token" output="带语义信息的 AST / IR" summary="确认代码结构合法，并判断它表达的操作是否有意义。">
      <Mechanism>字符流 ➜ Token ➜ AST ➜ 名称解析 ➜ 类型检查 ➜ IR</Mechanism>
      <Compare items={[{ label: '语法错误', rows: ['发现时机', '问题'], values: ['Parser', '结构不符合文法'] }, { label: '语义错误', rows: ['发现时机', '问题'], values: ['Semantic', '类型或名称不成立'] }]} />
      <Boundary>解析成功不等于行为正确；类型错误、未定义行为和资源问题仍可能在后续阶段出现。</Boundary>
    </ConceptNode>
    <ConceptNode id="codegen" title="IR 优化与代码生成" level="L2" parent="compile" input="AST / IR" output="机器码与重定位信息" summary="优化中间表示，并根据目标 ISA 发射指令。">
      <DecisionMatrix title="优化等级取舍" headers={['选项', '目标', '代价']} rows={[['-O0', '调试', '代码慢'], ['-O2', '通用发布', '编译更久'], ['-O3', '激进优化', '需评估体积与数值行为']]} />
      <Counterexample title="未定义行为不是可移植语义">不能把一次优化构建中的机器码行为当作语言规范保证。</Counterexample>
    </ConceptNode>

    <ConceptNode id="link" title="链接阶段" level="L1" parent="program-runtime" input="目标文件与库" output="可执行文件" summary="解析跨文件符号，合并段并生成可装载映像。">
      <Mechanism>读取目标文件 ➜ 符号解析 ➜ 地址分配 ➜ 重定位 ➜ ELF / PE</Mechanism>
      <RelationMap title="链接器解决的问题" items={[{ from: 'main.o', type: '引用', to: 'helper.o' }, { from: 'app', type: '依赖', to: 'libc.so' }, { from: '符号', type: '定位', to: '虚拟地址' }]} />
      <Boundary>链接成功不保证运行时共享库一定可找到，架构和 ABI 不兼容也可能导致启动失败。</Boundary>
    </ConceptNode>
    <ConceptNode id="runtime" title="运行阶段" level="L1" parent="program-runtime" input="可执行文件与环境" output="进程状态和 I/O" summary="操作系统创建进程，加载依赖，并在调度下推进执行。">
      <Timeline events={[{ label: '装载', content: '建立虚拟地址空间' }, { label: '初始化', content: '准备运行时与线程' }, { label: '执行', content: 'CPU 执行用户代码' }, { label: '退出', content: '交付退出码' }]} />
      <Insight title="环境也是输入" tone="success">当前目录、环境变量、时钟、随机源、文件系统和网络状态都会影响结果。</Insight>
      <Children><ConceptRef id="loader" /><ConceptRef id="execute" /></Children>
    </ConceptNode>
    <ConceptNode id="loader" title="装载与虚拟内存" level="L2" parent="runtime" summary="把文件段和共享库映射进进程地址空间"><Mechanism>校验格式 ➜ 映射代码与数据 ➜ 准备栈堆 ➜ 加载共享库 ➜ 设置入口</Mechanism></ConceptNode>
    <ConceptNode id="execute" title="CPU 取指与执行" level="L2" parent="runtime" summary="CPU 按 ISA 语义推进指令，同时由微架构优化实际执行。"><Flow steps={['Fetch', 'Decode', 'Schedule', 'Execute', 'Commit']} /></ConceptNode>

    <Relation from="compile" to="link" type="precedes" label="提供目标文件" />
    <Relation from="link" to="runtime" type="precedes" label="生成可装载映像" />
    <Relation from="parse" to="codegen" type="produces" label="提供带语义信息的 IR" />
    <Relation from="loader" to="execute" type="precedes" label="准备执行环境" />
    <Relation from="codegen" to="execute" type="depends-on" label="遵守 ISA 契约" />
  </ConceptGraph>
</ExplainPage>

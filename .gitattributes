# Keep one line ending everywhere. The repository stores text as LF; without
# this file a Windows checkout (core.autocrlf=true) rewrites files to CRLF and
# git warns on every diff.
* text=auto eol=lf
